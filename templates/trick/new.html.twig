{% extends 'base.html.twig' %}

{% block title %}New Trick{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-4">Ajouter une figure</h1>

        {{ include('trick/_form.html.twig') }}

        <div class="d-flex justify-content-end align-items-end">
            <a class="btn btn-secondary mb-3" href="{{ path('app_trick_index') }}">Retour à la liste des figures</a>
        </div>
    </div>



{% endblock %}
